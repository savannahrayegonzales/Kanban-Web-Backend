/**
 * Backend API Unit Tests Runner
 * Please keep tests in a separate file 
 * based on what they test and add them
 * here
 */

const assert = require("assert"),
      dataValidatorTest = require("./dataValidatorTest");

dataValidatorTest.checkEmail();

